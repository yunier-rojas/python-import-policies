release:
  stage: release
  before_script:
    - pip install poetry==1.6.1
  script:
    - git remote set-url --push origin https://gitlab-ci-token:${SOMBRA_BOT_TOKEN}@gitlab.com/$CI_PROJECT_PATH.git
    - git remote show origin
    - make release
  only:
    - main
