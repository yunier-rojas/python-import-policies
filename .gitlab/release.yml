release:
  image: node:latest
  stage: release
  variables:
    GITLAB_TOKEN: ${{ GITLAB_TOKEN }}
    PYPI_TOKEN: ${{ PYPI_TOKEN }}
  before_script:
    - npm install
  script:
    - npx semantic-release
