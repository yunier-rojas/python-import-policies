release:
  stage: release
  image: python:3.11
  variables:
    GIT_STRATEGY: clone
  before_script:
    - pip install poetry==1.6.1
    - poetry install --with=ci
    - git checkout -B "$CI_BUILD_REF_NAME" "$CI_BUILD_REF"
  script:
    - make release
  only:
    - main
